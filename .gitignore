# macOS
.DS_Store
.AppleDouble
.LSOverride

# Dependencies (commit package-lock.json so CI can use npm ci)
node_modules/

# Claude metadata (local only — except shared commands/skills)
.claude/*
!.claude/commands/

# Editor
.vscode/
.idea/
*.swp
*.swo

# Server — never commit secrets or database
server/.env
server/data/
